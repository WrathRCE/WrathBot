const { Client, RichEmbed } = require("discord.js");
let utils = require("../../utils/utils.js");

module.exports = {
	name: "dmall",
	description: "dms all",
	category: "dev",
	run: async (bot, message, args,) => {
        
    }
}